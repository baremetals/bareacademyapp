query RecentCourses($pagination: PaginationArg, $sort: [String]) {
  courses(pagination: $pagination, sort: $sort) {
    data {
      id
      attributes {
        slug
        title
        price
        duration
        image
        updatedAt
      }
    }
  }
}

query Courses($pagination: PaginationArg, $sort: [String]) {
  courses(pagination: $pagination, sort: $sort) {
    data {
      id
      attributes {
        slug
        title
        duration
        introduction
        description
        level
        price
        soloPrice
        isFree
        image
      }
    }
  }
}
